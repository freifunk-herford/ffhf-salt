Wir stellen sicher, dass das temopräre System Verzeichnis ``tmp`` besteht und über die korrekten Benutzerrechte verfügt.

Die kann mit den folgenden Befehlen erreicht werden.

.. code:: bash

	mkdir /tmp
	chown root:root
	chmod 1777

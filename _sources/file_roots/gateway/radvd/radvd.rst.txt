Installation
------------

.. code:: bash

    apt-get install radvd

Konfiguation
------------

Die Datei ``/etc/radvd.conf`` mit folgendem Inhalt anlegen.

.. literalinclude:: ../../../../../../file_roots/gateway/etc/radvd.conf
   :language: jinja

Todo: Ändert sich das Prefix oder ist das bei allen Gateways gleich?
